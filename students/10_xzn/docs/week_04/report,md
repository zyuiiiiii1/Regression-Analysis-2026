# 线性回归模型求解器性能对比实验报告

## 1. 实验设置
- **样本量 (N)**: 10,000
- **低维特征数 (P)**: 10
- **高维特征数 (P)**: 2,000
- **评价指标**: 运行耗时 (Execution Time)

## 2. 耗时对比实验结果

| 求解器类型 | 实验 A (P=10) 耗时 | 实验 B (P=2000) 耗时 |
| :--- | :--- | :--- |
| **Custom_Analytical** (手写解析解) | ~0.003s | ~2.40s |
| **Custom_GD** (手写梯度下降) | ~0.022s | ~1.55s |
| **Statsmodels OLS** | ~0.015s | ~3.20s |
| **Sklearn LinearRegression** | ~0.005s | ~2.10s |
| **Sklearn SGDRegressor** | ~0.008s | ~0.11s |

## 3. 思考题回答

### Q1: 在高维场景下，哪个 API 极其缓慢甚至可能崩溃？为什么？
**回答**：在高维场景（P=2000）下，**Statsmodels OLS** 和 **手写解析解 (AnalyticalSolver)** 表现得最为缓慢。
这是因为解析解方法的核心是求解正规方程，涉及到矩阵 $X^T X$ 的运算和线性方程组求解。该矩阵的大小为 $(P+1) \times (P+1)$，其计算复杂度大约为 $O(P^3)$。当特征数 $P$ 增加时，计算量会呈立方级增长，导致高维场景下耗时剧增。

### Q2: 为什么 SGDRegressor 能在极短时间内完成任务？
**回答**：**SGDRegressor**采用的是迭代优化策略，而不是一次性计算复杂的矩阵逆。
1. **计算复杂度低**：SGD每次更新参数只需要计算一小部分样本的梯度
2. **内存友好**：不需要存储巨大的协方差矩阵，效率远超基于解析公式的求解器。